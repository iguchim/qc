<%= form_for [@customer, @product], :html => {:class => "form-horizontal"} do |f| %>
    <%= render 'fields', f: f %>
    <%= f.submit '製品追加',class: 'col-xs-offset-2 btn btn-primary' %>
    <%= link_to '戻る', @customer, class: 'btn btn-primary' %>
<% end %>