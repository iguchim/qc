<%= form_for @product, :html => {:class => "form-horizontal"} do |f| %>
  <%= render 'fields', f: f %>
  <%= f.submit '更新',class: 'col-xs-offset-2 btn btn-primary' %>
  <%= link_to '戻る', @product, class: 'btn btn-primary' %>
<% end %>