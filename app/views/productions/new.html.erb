<%= form_for [@production.product, @production], :metohod => :post, :html => {:class => "form-horizontal"} do |f| %>
    <%= render 'fields', f: f %>
    <%= f.submit '追加',class: 'col-xs-offset-2 btn btn-primary' %>
    <%= link_to '戻る', @production.product, class: 'btn btn-primary' %>
<% end %>
