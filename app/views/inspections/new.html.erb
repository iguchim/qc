<!-- Study the following pattern!!! -->
<%= form_for([@product, @product.inspections.build], html:{ class: 'form-horizontal'}) do |f| %>
    <%= render 'fields', f: f %>
    <%= f.submit '項目追加',class: 'col-xs-offset-2 btn btn-primary' %>
    <%= link_to '戻る', product_path(@product), class: 'btn btn-primary' %>
<% end %>